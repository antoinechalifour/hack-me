import styled from "styled-components";

export const Button = styled.button`
  padding: 1rem 1.5rem;
  border-radius: 1rem;
  border: none;

  background: indigo;
  color: #fff;

  font-family: inherit;
  font-size: 1.4rem;
  font-weight: bold;
  text-transform: uppercase;

  cursor: pointer;
`;
